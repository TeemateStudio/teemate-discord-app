export type Game = {
  label: string; // Le nom d'affichage du jeu.
  value: string; // La valeur interne pour le jeu.
};

export type Mode = {
  label: string; // Le nom d'affichage du mode de jeu.
  value: string; // La valeur interne pour le mode de jeu.
};