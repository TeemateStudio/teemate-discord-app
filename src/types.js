/**
 * @typedef {object} Game
 * @property {string} label - Le nom d'affichage du jeu.
 * @property {string} value - La valeur interne pour le jeu.
 * @property {Mode[]} modes
 */

/**
 * @typedef {object} Mode
 * @property {string} label - Le nom d'affichage du mode de jeu.
 * @property {string} value - La valeur interne pour le mode de jeu.
 */

export {}; // Nécessaire pour que le fichier soit traité comme un module et que les @typedef soient globaux.